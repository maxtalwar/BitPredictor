import tkinter as tk
import os

# creates a starting window
window = tk.Tk()

# Adds a header to the window
header = tk.Label(text="BitTrader")
header.pack()

# make the window actually do something
window.mainloop()
